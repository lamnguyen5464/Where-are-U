{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "all": "rm -rf package-lock.json && rm -rf yarn.lock && rm -rf node_modules/ && yarn && yarn start",
    "start": "nodemon src/index.js"
  },
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "debounce": "^1.2.1",
    "express": "^4.17.1",
    "lodash": "^4.17.21",
    "nodemon": "^2.0.4",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "prettier": "2.3.1"
  }
}
